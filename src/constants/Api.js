// TODO: env 파일의 todo mall api endpoint 끝에 / 를 붙인 이유가 따로 있는지
// 없다면 가독성을 위해서 env에서 선언한 api endpoint 끝의 /를 삭제하고 , 사용처에서 붙여주는게 가독성을 좀 더 높힐거같다.
export const API_ENDPOINT = process.env.REACT_APP_TODO_MALL_API_ENDPOINT;
